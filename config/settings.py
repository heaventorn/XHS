# -*- coding: utf-8 -*-
"""config/settings.py — 大爬虫框架配置

集中管理：双密码验证参数、输出路径、启用的平台采集器。
各平台具体参数（关键词、采集上限）在各采集器内维护，此处只做开关。
"""
import os

# ==================== 双密码验证 ====================
# 手动访问密码（BAT 弹窗输入）
MASTER_PWD = "ACFUND"
# Argon2 组合哈希 = Argon2("ACFUND" + 本地 pwd.key 强密码)，程序外一次性生成（16MB 内存档）
# 与 XHS 原版一致，pwd.key 中为同一强密码，保证框架版与旧版密码体系互通
ARGON2_HASH = "$argon2id$v=19$m=16384,t=2,p=1$HCgmuwQA+BfTOr27ADdlVQ$LazRy4nOadH7YkzrWLj3gA2uOLA+CbSs6zZD+Y5sR+o"
# 本地强密码文件名（仅存本地、不上传仓库）
KEY_FILE = "pwd.key"

# ==================== 输出 ====================
# 输出目录（默认桌面）
OUTPUT_DIR = os.path.join(os.path.expanduser("~"), "Desktop")
# 输出文件名前缀
OUTPUT_PREFIX = "大爬虫采集结果"

# ==================== 目标公司列表（联系池采集器查询对象） ====================
# 企查查/天眼查/IT桔子/脉脉 等联系池采集器会逐个查询这些公司。
# 在此填写你想找 CFO/融资负责人的公司名（A-C 轮非上市公司优先）。
# 留空则联系池采集器不运行（仅小红书线索池运行）。
TARGET_COMPANIES = [
    "航天驭星",       # 商业航天·C轮·卫星测运控
    "银河航天",       # 商业航天·C轮·卫星制造
    "小鹏汇天",       # 低空经济·B轮·飞行汽车
    "沃兰特航空",     # 低空经济·C+轮·eVTOL
    "自变量机器人",   # 高端装备·A++轮·人形机器人
]

# ==================== 平台开关 ====================
# 按需启用的采集器（True=启用）。每个启用平台 = 一个并行 Worker 进程。
# 新增平台：在 platforms/ 下创建 <name>.py（run() 返回行列表 + --output 支持），并在此追加。
ENABLED_PLATFORMS = {
    "xhs": True,          # 小红书（线索池：帖子/账号，联系方式留空待人工补）
    "qcc": True,          # 企查查（联系池：工商电话/邮箱/法人/注册资本）
    "tianyancha": True,   # 天眼查（联系池：工商电话/邮箱/法人/注册资本，未登录也可采集部分字段）
    "itjuzi": True,       # IT桔子（联系池·融资数据：轮次/金额/投资方/行业，需登录）
    "maimai": True,       # 脉脉（线索池·高管人脉：CFO/董秘/融资负责人姓名职位，需登录）
    # "zhihu": False,
    # "bilibili": False,
}
